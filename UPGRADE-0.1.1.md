# ArtLens 0.1.1 修复补丁

1. 在原运行终端按 Ctrl+C 停止服务。
2. 备份原项目的 artlens 文件夹。
3. 将本补丁的 artlens 文件夹复制到原项目根目录（包含 .env、data、requirements.txt 的目录），合并文件夹并替换同名文件。不要把整个补丁放进新的子目录运行。
4. tests 文件夹可同步合并，用于开发验证。
5. 保留原 .env、.venv 和 data。本补丁不包含这些目录或密钥，不需要安装新依赖，不需要重建索引。
6. 原目录运行：

```bat
.venv\Scripts\python -m uvicorn artlens.main:app --host 127.0.0.1 --port 8000
```

7. 浏览器按 Ctrl+F5 强制刷新，再重新上传图片。
8. 可访问 http://127.0.0.1:8000/api/health 检查 version 是否为 0.1.1；不要仅依据旧页面判断补丁是否生效。

## 变化

高分候选（默认 0.95，领先第二名 0.03；可通过现有 .env 覆盖）进入双图核验。阈值仅是未校准的请求分流参数。将上传图与 data/images 中的第一名参考图一起发送给现有视觉 API；至少两项具体对应细节且没有矛盾才接受模型的 same 判断。通过时显示“很可能匹配”，直接从馆藏元数据展示作品名、作者和年代。不同则返回未知，缺图、格式无效或服务失败则保持待核验。

这会对达到门槛的查询增加一次视觉 API 请求和相应费用。是模型视觉复核，不是确定性验证或实物真伪鉴定；仍可能误判。默认门槛不会把相似度解释为正确概率。尚未做准确率校准。

候选缩略图改用本地已下载文件，避免浏览器直连馆藏站点失败。.env 优先于同名进程环境变量，重启后生效。前端静态脚本新增版本参数。

## 验证

15 项本地测试通过，包括同图/异图/不确定分流、格式异常、证据不足、本地参考图路由与路径校验；JavaScript 语法检查通过。双图模型测试使用替身响应，没有你的 API 密钥，尚未测试真实双图调用或浏览器视觉效果。

旧 README 中关于永远只输出候选、空阈值始终拒绝识别的描述已由本补丁规则替代。其余原版安装说明仍适用。
