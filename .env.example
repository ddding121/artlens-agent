# 使用支持图片输入的 OpenAI 兼容 Chat Completions API。
# 以下三项均需填写；不要把 .env 或密钥上传 GitHub。
VISION_API_KEY=
VISION_BASE_URL=
VISION_MODEL=
# 留空采用未校准的双图核验分流默认值：0.95 / 0.03。正式评估请先在验证集校准。
MATCH_MIN_SCORE=
MATCH_MIN_MARGIN=
